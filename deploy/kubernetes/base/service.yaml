apiVersion: v1
kind: Service
metadata:
  name: continua
spec:
  selector:
    app: continua
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: proxy
      port: 8081
      targetPort: 8081
  type: ClusterIP
